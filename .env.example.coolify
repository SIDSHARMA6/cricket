# Coolify Environment Variables for Production

# Server Configuration
HOST=0.0.0.0
PORT=1337
NODE_ENV=production

# Security Keys (Generate secure random values - use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
APP_KEYS=key1,key2,key3,key4
API_TOKEN_SALT=your-api-token-salt
ADMIN_JWT_SECRET=your-admin-jwt-secret
TRANSFER_TOKEN_SALT=your-transfer-token-salt
ENCRYPTION_KEY=your-encryption-key
JWT_SECRET=your-jwt-secret

# PostgreSQL Database (Coolify will provide these)
DATABASE_CLIENT=postgres
DATABASE_URL=${DATABASE_URL}
DATABASE_HOST=${POSTGRES_HOST}
DATABASE_PORT=${POSTGRES_PORT:-5432}
DATABASE_NAME=${POSTGRES_DB}
DATABASE_USERNAME=${POSTGRES_USER}
DATABASE_PASSWORD=${POSTGRES_PASSWORD}
DATABASE_SSL=false

# Optional: Cloudinary for media uploads
CLOUDINARY_NAME=your-cloud-name
CLOUDINARY_KEY=your-api-key
CLOUDINARY_SECRET=your-api-secret

# CORS (set to your frontend URL)
CORS_ORIGIN=https://your-app.com
